experimental = ["benchmarks"]

[profile.default]
slow-timeout = { period = "1s", terminate-after = 8 }
bench.global-timeout = "2h"

[[profile.default.overrides]]
filter = "test(bench_commands)"
bench.slow-timeout = { period = "60s", terminate-after = 10 }