info:
  name: delete_label
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{TASKFLOW_BASE_URL}}/labels/{{CREATED_LABEL_ID}}"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
