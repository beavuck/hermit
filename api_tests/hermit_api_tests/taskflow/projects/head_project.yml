info:
  name: head_project
  type: http
  seq: 4

http:
  method: HEAD
  url: "{{TASKFLOW_BASE_URL}}/projects/{{PROJECT_ID}}"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
