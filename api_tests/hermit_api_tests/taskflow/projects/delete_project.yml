info:
  name: delete_project
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{TASKFLOW_BASE_URL}}/projects/{{PROJECT_ID}}"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
