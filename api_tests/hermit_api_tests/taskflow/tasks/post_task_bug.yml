info:
  name: post_task_bug
  type: http
  seq: 3

http:
  method: POST
  url: "{{TASKFLOW_BASE_URL}}/projects/{{PROJECT_ID}}/tasks"
  headers:
    Content-Type: application/json
  body:
    type: json
    data: |-
      {
        "title": "Fix allOf resolution panic",
        "type": "bug",
        "severity": "high"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "201"
    - expression: res.body.id
      operator: isDefined
    - expression: res.body.title
      operator: isDefined
    - expression: res.body.type
      operator: eq
      value: "bug"
    - expression: res.body.severity
      operator: isDefined
    - expression: res.body.projectId
      operator: isDefined
    - expression: res.body.createdAt
      operator: isDefined
    - expression: res.body.updatedAt
      operator: isDefined

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
